# Copy to .env.local for local development overrides.
# Vite proxies /api → VITE_API_BASE_URL in dev mode (see vite.config.ts).
# In production the nginx.conf proxy handles /api → alerting-service:9005.

VITE_API_BASE_URL=http://localhost:9005
